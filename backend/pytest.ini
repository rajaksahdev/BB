[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra -q
# Put backend/ on sys.path so `from tests.conftest import ...` works under a
# bare `pytest` invocation (CI), not just `python -m pytest`.
pythonpath = .
