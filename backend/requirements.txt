# --- Phase 0: Foundation (API + DB + migrations) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Phase 1: data engine ---
httpx==0.28.1

# --- Phase 2: backtest core ---
backtesting==0.6.5
pandas==3.0.4
numpy==2.5.0

# --- Phase 3: auth ---
PyJWT==2.10.1
cryptography==49.0.0  # ES256 verification of Supabase JWKS-signed tokens

# --- Phase 5: billing (Lemon Squeezy) ---
# No official Python SDK; called over REST with httpx (pinned above).
